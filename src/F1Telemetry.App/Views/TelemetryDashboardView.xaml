<UserControl x:Class="F1Telemetry.App.Views.TelemetryDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:F1Telemetry.App.Views" 
             xmlns:viewmodels="clr-namespace:F1Telemetry.App.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:TelemetryDashboardViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <!-- Driver selector -->
            <ComboBox Grid.Row="0"
                      Width="200"
                      Margin="10"
                      HorizontalAlignment="Left"
                      ItemsSource="{Binding Drivers}"
                      SelectedItem="{Binding SelectedDriver}"
                      DisplayMemberPath="Driver"/>
        </StackPanel>

        <!-- Telemetry -->
        <!--<views:TelemetryView Grid.Row="1"/>-->
    </Grid>

</UserControl>
