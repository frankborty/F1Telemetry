<UserControl x:Class="F1Telemetry.App.Views.TelemetryDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:F1Telemetry.App.Views"
             xmlns:viewmodels="clr-namespace:F1Telemetry.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:TelemetryDashboardViewModel}"
             mc:Ignorable="d">

    <Border Background="{StaticResource CardBrush}" CornerRadius="8" Padding="4">
      <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10">
            <Button DockPanel.Dock="Right"
                    Content="X"
                    Width="25" Height="25"
                    FontSize="12"
                    Command="{Binding RemoveCommand}"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"/>

            <StackPanel Orientation="Horizontal">
              <ComboBox Width="220"
                      HorizontalAlignment="Left"
                       ItemsSource="{Binding Drivers}"
                       SelectedItem="{Binding SelectedDriver}"
                       Background="White" Foreground="#111827">
                <ComboBox.ItemTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding Driver}" Foreground="#111827" Background="White"/>
                  </DataTemplate>
                </ComboBox.ItemTemplate>
                <ComboBox.ItemContainerStyle>
                  <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="#111827"/>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Padding" Value="6,4"/>
                  </Style>
                </ComboBox.ItemContainerStyle>
              </ComboBox>
              <TextBlock Text="{Binding TelemetryVM.Telemetry.DriverName}" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </StackPanel>
        </DockPanel>

        <views:TelemetryView
            Grid.Row="1"
            DataContext="{Binding TelemetryVM}" />
      </Grid>
    </Border>
</UserControl>
