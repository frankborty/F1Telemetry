<UserControl x:Class="F1Telemetry.App.Views.TelemetryDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:F1Telemetry.App.Views"
             xmlns:viewmodels="clr-namespace:F1Telemetry.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:TelemetryDashboardViewModel}"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <views:TelemetryView
            Grid.Row="1"
            DataContext="{Binding TelemetryVM}" />
        
    </Grid>
</UserControl>