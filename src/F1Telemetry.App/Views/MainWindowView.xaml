<Window x:Class="F1Telemetry.App.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:F1Telemetry.App"
        xmlns:viewmodels="clr-namespace:F1Telemetry.App.ViewModels"
        xmlns:converters="clr-namespace:F1Telemetry.App.Converters" xmlns:views="clr-namespace:F1Telemetry.App.Views"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="F1 Telemetry" Height="720" Width="1200"
        MinHeight="450" MinWidth="650"
        Background="{StaticResource WindowBrush}">

    <Window.Resources>
        <converters:BoolToBrushConverter x:Key="BoolToBrushConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10">
            <Button DockPanel.Dock="Right" Content="+ Dashboard" Command="{Binding AddDashboardCommand}"/>
            <TextBlock Text="F1 TELEMETRY" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="  |  " Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ConnectionStatus}" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding ConnectionMessage}" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Margin="8,0,0,0"/>
        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <ItemsControl ItemsSource="{Binding TelemetryDashboardVMList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                 <WrapPanel HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                     <views:TelemetryDashboardView DataContext="{Binding}" Width="380" Margin="6"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
